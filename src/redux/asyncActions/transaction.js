import { createAsyncThunk } from "@reduxjs/toolkit";

export const getTransaction = createAsyncThunk('transfer/history', async () => {
  const result = {};
  try {
    const 
  } catch (error) {
    
  }
})